# Vercel Postgres 연결 정보
# Vercel Dashboard > Storage > Postgres에서 생성 후 복사

# Pooled Connection (일반 쿼리용)
DATABASE_URL="postgres://default:xxxxx@xxx.postgres.vercel-storage.com:5432/verceldb?sslmode=require&pgbouncer=true&connect_timeout=15"

# Direct Connection (마이그레이션용)
DIRECT_URL="postgres://default:xxxxx@xxx.postgres.vercel-storage.com:5432/verceldb?sslmode=require&connect_timeout=15"
